Collection
   │
   └── .stream()
   
Array
   │
   └── Arrays.stream()

Individual values
   │
   └── Stream.of()

Generate values
   │
   └── Stream.generate()

Generate sequential values
   │
   └── Stream.iterate()

Primitive numbers
   │
   ├── IntStream
   ├── LongStream
   └── DoubleStream